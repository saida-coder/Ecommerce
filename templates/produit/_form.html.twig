{{ form_start(form) }}
    {{ form_row(form.article) }}<br>
    {{ form_row(form.descreption) }}<br>
  {{ form_row(form.price) }}<br>
    {{ form_row(form.image) }}
     
    {{ form_widget(form) }}<br>
    <button class="btn btn-success">{{ button_label|default('Save') }}</button><br>
{{ form_end(form) }}
