{% extends "fichier.html.twig" %}
  
{% block main %}
    
   
    
<div class="container">
<div class="row">
 <div class="col-6 col-md-4">
        <div class="list-group">
         
            <a href="vendeur/{{ users.id }}" class="list-group-item">Liste des articles </a>
            
        <a href="{{ path('vendeur_new') }}"class="list-group-item">Ajouter article</a>
          </div>
        </div>
            
 <div class="col">
 <table class="table table-striped">
  <thead>
    <tr>
        
 <th scope="col">image</th>
       <th scope="col">id</th>

      <th scope="col">nom</th>
      <th scope="col">descreption</th>
       <th scope="col">prix</th>
      <th scope="col">catégorie</th>
<th scope="col">Action</th>

      

    </tr>
  </thead>
  <tbody>
  <h2>liste des produit </h2><br><br>
 {% for vendeur in vendeurs %}

        <tr>
        <td> <img src="{{ asset(vendeur.image) }}" alt="" width="60"> </td>
                <td>{{ vendeur.id }}</td>

        <td>{{vendeur.article }}</td>
        <td>{{ vendeur.description }}</td>
        <td>{{ vendeur.price }}</td>
        <td>{{vendeur.relation.nom }}</td>

<td> 

<a href="/vendeurdelate/{{ vendeur.id }}" >
<span >&#10008;</span></a>

</td>

        </tr>
        

        
           {% endfor %}
  </table>       
                   
          
{% endblock %}